# Computer Set-up Files

Use `ln -s ~/code/dotfiles/.zprofile` in the home directory to utilize the [.zprofile](./.zprofile) created in this repo.

After installing [Homebrew](https://brew.sh), run `brew bundle --file Brewfile`.
